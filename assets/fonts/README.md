# Santander Font Assets

Official fonts currently bundled in the app:

- `SantanderHeadline-Bold.otf`
- `SantanderText-Regular.otf`
- `SantanderText-Bold.otf`

These are loaded in `src/constants/typography.ts` and the app waits for them
before rendering the main UI.
